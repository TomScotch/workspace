for f in /opt/scps/*.wav
  do
    if [ -f "$f.mp4" ]
      then
        echo "skipped : " $f " : already existing"
    else
     ffmpeg -i $f -f image2 -i $(shuf -n1 -e /opt/sheep/*.png) \
     -c:v libx264 -preset medium \
     -shortest -fflags +genpts $f.mp4
    fi
done
